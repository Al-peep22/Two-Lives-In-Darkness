using UnityEngine;

//Many functions ready to be called to initiate a screen animation like fade in, fade out, slide in, slide out, etc. This will be used to make the game look more polished and professional. The functions will be called from other scripts when needed.
//Open/Close_Eyes: One Animation should be the wake up animation where the screen starts black and then moves the darkness out to resemble the player opening their eyes.
//Room_Transition: Another animation should be the room transition animation where the screen fades to black and then fades back in to the new room. This will be used when the player moves from one room to another.
public class ScreenAnimations : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
